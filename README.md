# GCS Language Extension

Provides syntax highlighting and commands for `.gcs` script files.

## Features

- Syntax highlighting for GCS commands like MOV, ANI, WAIT, SND, DO.
- Activate the extension manually via command palette.